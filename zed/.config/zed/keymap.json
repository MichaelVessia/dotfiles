// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Editor && vim_mode != insert",
    "bindings": {
      "space t": "terminal_panel::ToggleFocus",
      "space e": "project_panel::ToggleFocus",
      "space g": "git_panel::ToggleFocus",
      "space i": "assistant::ToggleFocus",
      "space o": "outline_panel::ToggleFocus"
    }
  },
  {
    "context": "Editor && vim_mode == normal",
    "bindings": {
      // Tabs / Buffers
      "space tab": "tab_switcher::Toggle",
      // Find things
      "space f f": "file_finder::Toggle",
      "space f b": "editor::FindAllReferences",
      // Code actions
      "space c a": "editor::ToggleCodeActions",
      // UI changes
      "space u d": "editor::ToggleInlineDiagnostics",
      "space u l": "editor::ToggleLineNumbers",
      "space u r l": "editor::ToggleRelativeLineNumbers",
      "space u h": "editor::ToggleInlayHints"
    }
  },
  // These binds cause problems because "Workspace" is too wide of a scope
  // Need to solve this so I can toggle panes
  // {
  //   "context": "Workspace",
  //   "bindings": {
  //     "space x l": "workspace::ToggleLeftDock",
  //     "space x b": "workspace::ToggleBottomDock",
  //     "space x r": "workspace::ToggleRightDock"
  //   }
  // },
  {
    "context": "Dock",
    "bindings": {
      // Allow movement in/out of the dock, i.e. terminal
      // Generally i imagine i will just have it closed, but may still be useful.
      "ctrl-w h": "workspace::ActivatePaneLeft",
      "ctrl-w l": "workspace::ActivatePaneRight",
      "ctrl-w k": "workspace::ActivatePaneUp",
      "ctrl-w j": "workspace::ActivatePaneDown"
    }
  }
]
